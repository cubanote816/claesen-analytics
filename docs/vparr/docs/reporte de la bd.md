Project Status Reverse Engineering Report
Summary of Investigation
I have performed an exhaustive "black box" analysis of the CAFCA SQL Server database to decode the meaning of the state column in the
project
table. Despite searching through 240+ tables and multiple schemas, no explicit lookup table (e.g., status_labels) was found.

Key Findings

1. Database Evidence
   State Distribution: There are 21 unique integer values in the state column.
   Top Frequencies:
   0: ~1,782 projects (Likely "Active" or "New")
   3: ~497 projects
   18: ~272 projects
   15: Often associated with fl_active = 0 (Likely "Finished/Closed")
   Keywords: A global search for the Dutch term "Afgewerkt" (Finished) across all text columns only revealed it in estimate_item.descr, suggesting it's used as a manual note rather than a system-wide status label.
2. Correlation Analysis
   State ID fl_active Observations Likely Meaning
   0 1 Majority of projects, including old ones. Standard Active / General
   3 1 Frequent in recent projects. In Execution / Planned
   15 0 Correlates with fl_active = 0. Completed / Archived
   13 1 Seen in very recent projects (2025). New / Intake
3. Conclusion
   The legacy CAFCA system likely handles these status mappings in one of two ways:

Hardcoded in the Binaries: The mappings exist only in the desktop application's code.
Implicit Logic: The status is derived from a combination of flags (fl_active, fl_locked) and dates.
Recommended Action
Since we cannot find a "Source of Truth" in the DB, I propose:

Define a "Smart Status" Badge: Instead of showing the raw integer, we show a badge based on the fl_active and state correlation we found.
User Validation: Present these findings to the Claesen team to confirm if 15 indeed means "Finished" in their daily usage.
Proposed UI Mapping (For Filament):
fl_active = 1 AND state = 0, 3, 13 -> Actief (Success)
fl_active = 0 OR state = 15 -> Afgewerkt (Gray)
fl_locked = 1 -> Gearchiveerd (Warning)
Report generated by Antigravity Lead Architect
