# Programa multiempresa Electro Bertels — histórico de handoff

> Entradas de `handoff.md` escritas en `electrobertels/trunk` (2026-09-17 → 2026-09-22), archivadas al reconciliar con `main` (que reorganizó handoff.md en tabla, CLA-536). No es lectura obligatoria.

> Última actualización: 2026-09-22 — **Programa multiempresa Electro Bertels: F2 — CLA-465 y CLA-464 Done (cierre de sus tramos parciales).** CLA-465: `activity_log` gana `site_id` (derivado del subject) y `correlation_id` (uno por request, nuevo middleware `AssignCorrelationId` + `CorrelationIdContext` scoped), `ActivityLogResource` de solo lectura para `super_admin`, y las dos alertas pendientes — `SuperAdminGrantedNotification` (concesión de `super_admin`) y `ConsultationExportPerformedNotification` (export de leads). **Bug real encontrado testeando la alerta de super_admin:** el propio `syncRoles()` de Spatie Permission llama a `$this->assignRole()` internamente, que PHP resuelve a la override de `User` — sin guard, re-sincronizar los MISMOS roles disparaba una alerta falsa; fix con un flag de reentrancia. **Hallazgo crítico, con blast radius sobre CLA-460/P4 (ya "Done"):** `AdminPanelProvider`/`BertelsPanelProvider` arman su propio array explícito de middleware en vez de usar el alias del grupo `web` de Laravel — `bootstrap/app.php`'s `$middleware->web(append: [...])` **nunca llegó a ninguna ruta de panel real**, así que `UpdateUserActivity`/`ResolveOrganizationContext` nunca corrieron ahí pese a estar documentados como cerrados. Probado con un `Cache::has()` real (el intento inicial de "confirmarlo" con `OrganizationContext::resolve()` directo era metodológicamente inválido — ese método siempre re-resuelve, sin importar si algún middleware corrió). Fix: los 3 middlewares ahora también van directo en el array explícito de ambos paneles. CLA-464: reautenticación MFA (`StepUpAuthenticator`, reutiliza los `getChallengeFormComponents()` nativos de Filament) para las 3 acciones destructivas que este programa construyó — cambio de rol (`EditUser::changeRolesAction()`, el `CheckboxList` de roles sale del form general), exportación CSV y erasure GDPR de leads — más sesiones revocables (`EditUser::revokeSessionsAction()`, borra filas de `sessions`). **Bug real de colisión de key:** `AppAuthentication`/`EmailAuthentication` usan ambos `'code'` como key de su campo — concatenar los dos challenge forms colisionaría; el servicio expone exactamente uno. Azure OAuth queda fuera a propósito (no pasa por el reto interactivo de Filament, solo por el enforcement de "panel exige MFA configurado", que sí aplica igual). Verificación: `ClaesenBaseline` 78/78 (381 assertions), `Modules/Core/tests` 262/262 (734 assertions), `Modules/Website/tests` 203/203 (581 assertions), suite dirigida 39/39. Snapshots de rutas/panel-registry regenerados de forma puramente aditiva. Pint: 0 deuda nueva (confirmado archivo por archivo contra HEAD). Detalle completo en `CLAUDE.md` → CLA-465 (cont.)/CLA-464 (cont.). Comiteado `ec6d7bb`. Sin push, sin deploy. Siguiente: CLA-461 (aislar destinatarios y eliminar notificaciones globales) y CLA-466 (aislar API/servicios/jobs/comandos/búsquedas/exportaciones), últimos dos tickets abiertos de F2.

> Última actualización: 2026-09-20 (anterior) — **Programa multiempresa Electro Bertels: F4 — CLA-475 Done (validación, antispam, rate limiting y consentimiento); CLA-474 (adjuntos) documentado como bloqueado, sin código.** `/consultations`/`/contact-email` no tenían límite de longitud, honeypot, rate limit ni consentimiento antes de este ticket — confirmado por auditoría, no asumido. **Longitud:** `message` gana tope configurable (5000 por defecto). **Honeypot** siempre activo (nombre del campo no publicado en el OpenAPI, solo el frontend real lo conoce) — un envío con el campo relleno se descarta en silencio (201 falso, nada persistido). **Cloudflare Turnstile inerte por defecto** (mismo patrón D4 de todo el programa): nunca llama a Cloudflare salvo que `secret_key` Y `enforce` estén ambos configurados; con enforce activo, falla **abierto** ante un error de red (nunca bloquea el formulario por un corte de Cloudflare) pero rechaza (422) una verificación real fallida, auditando el intento. **Rate limit por IP** (`throttle:10,1` config-driven) en ambas rutas — antes cero. **Consentimiento** (`consent_given_at`/`consent_policy_version`) opcional, nunca obligatorio (el frontend real no lo envía todavía). **Alertas de abuso** (`website_intake_spam_attempts` append-only + `website:check-intake-abuse-alerts`, programado cada hora, idempotente por bucket horario) — recipientes vía `->inOrganization()`, mismo patrón CLA-560. **Bug real encontrado y corregido en la verificación:** los tests nuevos fallaban 12/16 en la corrida de suite completa (0 en aislamiento) por el mismo patrón de contaminación cross-clase que CLA-478 ya documentó (`DatabaseTruncation` de otra clase comitea filas reales que sobreviven y contaminan la línea base de un test `RefreshDatabase` posterior) — fix: email único por test + filtro por email en vez de conteo absoluto de tabla, mismo remedio que CLA-478. 2 centinelas del baseline invertidos (rate limit real + presencia del middleware `throttle:`) + snapshot de rutas regenerado (diff puramente aditivo). 22 tests nuevos, regresión `Modules/Website` 203/203 (581 assertions), `Modules/Core`+`ClaesenBaseline` 316/316 (1064 assertions). Smoke real vía `artisan tinker`: submission real 201, honeypot 201-falso sin persistir, 10 requests reales de la misma IP → `201×10` luego `429`. **CLA-474 (cuarentena/antimalware de adjuntos):** auditoría confirmó que `ConsultationRequest` no tiene NINGUNA capacidad de adjuntos hoy — construir la mitad "segura" (allowlist/disco privado/URL firmada) sin la herramienta de antivirus elegida sería net-negative, no progreso parcial: expondría upload público sin escanear, el riesgo exacto que el ticket busca prevenir. Documentado en Linear como bloqueado por decisión externa (elección de herramienta AV), sin código; retomar en cuanto se decida. Detalle completo en `CLAUDE.md` → «F4 — Leads y privacidad» → CLA-475/CLA-474. **Con esto, F4 queda en su máximo avance posible sin decisiones externas del usuario.** Sin push, sin deploy.

> Última actualización: 2026-09-20 (anterior) — **Programa multiempresa Electro Bertels: F4 avanzando — CLA-476 Done (retención, exportación y derechos RGPD).** Antes de este ticket, `ConsultationRequest` no tenía ningún mecanismo de retención, exportación auditada ni erasure bajo demanda — los leads vivían para siempre, `name`/`email`/`phone`/`company` eran de solo lectura tras crearse (sin vía de rectificación RGPD). `Organization::retention_policy` (columna JSON inerte desde CLA-458/P1) gana su primer uso vía `retentionSetting()`; `spam_days`/`closed_anonymize_days` por organización con fallback global (`WEBSITE_RETENTION_ENABLED` default `false` en todo entorno). `ConsultationRequest::anonymize()` neutraliza PII vía `saveQuietly()` (deliberadamente sin disparar el observer normal). `RetentionService` borra en duro el spam vencido y anonimiza el closed vencido, todo auditado en `activity_log`; `ApplyRetentionPolicyCommand` (programado diario 05:00) fuerza dry-run si el flag global está apagado, sin importar el flag `--dry-run`. 2 permisos nuevos (`website.export-consultation-requests`/`website.erase-consultation-pii`, backfill vía migración + seeder, mismo patrón CLA-496) gatean una acción `export` (CSV streaming, auditado) y `erase` (bajo demanda) en el resource — **decisión deliberada de NO usar el subsistema nativo de Export de Filament v5** (exigiría migraciones/jobs vendor para un recurso de volumen modesto). Mitigación de CSV formula injection (`sanitizeCsvValue()`). **4 bugs reales encontrados y corregidos en la verificación:** `message` NOT NULL en el esquema original (`anonymize()` seteaba `null`, corregido a `''`); `eraseNow()` no aceptaba un `User->id` int real (ampliado a `int|string|null`, igual que Spatie `causedBy()`); `RetentionService::applyToSite()` reventaba `MissingOrganizationContext` en contexto de comando sin request (fix `withoutGlobalScope('site')`, mismo patrón CLA-472); el test del export tomaba la entrada de `activity_log` equivocada porque `LogsActivity` ya había auto-logueado el "created" del factory. 19 tests nuevos (`RetentionPolicyTest`, harness CLA-457, sin datos reales de Bertels — D10). Regresión `Modules/Website` 181/181 (509 assertions), `Modules/Core`+`ClaesenBaseline` 316/316 (1065 assertions). **Un criterio del ticket NO resuelto por código, documentado como gate de pre-lanzamiento:** validación jurídica belga de los plazos de retención por defecto, antes de activar el flag en producción. Detalle completo en `CLAUDE.md` → «F4 — Leads y privacidad» → CLA-476. **Con esto, de los tickets implementables sin decisión externa, F4 solo deja CLA-474 (cuarentena/antimalware) y CLA-475 (antispam/Turnstile) pendientes de decisión del usuario.** Sin push, sin deploy.

> Última actualización: 2026-09-20 (anterior) — **Programa multiempresa Electro Bertels: F4 avanzando — CLA-477 Done (bandeja de leads aislada y flujo comercial).** `ConsultationRequest` ya tenía casi todo el andamiaje CRM (`status`/`priority`/`assigned_to`/`internal_notes`/`tags` JSON) desde el sprint original de Website, pero el set de `status` era genérico (pending/contacted/in_progress/completed/cancelled), `tags` no tenía UI, no había noción de SLA, y `assigned_to` no verificaba nada sobre a quién se podía asignar un lead. **Estados nuevo/asignado/en curso/esperando cliente/cerrado/spam** reemplazan el set viejo vía 2 migraciones (estructura + datos, D7) — mapeo documentado (`pending→new`, `contacted→assigned`, `completed`/`cancelled`→`closed`, nunca `spam` retroactivo) verificado end-to-end contra MySQL real con filas legacy insertadas a mano, migradas y revertidas. **SLA de primera respuesta medible** vía `first_response_at`, estampado en los 2 puntos de escritura que debían quedar sincronizados (`ConsultationService::updateStatus()` y `ConsultationRequestObserver::saving()`, el guardado real de Filament). **Responsable debe pertenecer a la misma empresa** — guard en el observer, gateado por `organizations.enforce` (D4, inerte hoy), doble candado con el picker de Filament ya scoped. **Bug real encontrado en verificación manual, no por los tests iniciales:** el auto-avance `new→assigned` al asignar mutaba `status` DESPUÉS de que el chequeo de estampado de SLA ya había corrido dentro del mismo `saving()` — asignar un lead nuevo lo movía a `assigned` con `first_response_at` en `null`. Fix: reordenar dentro del observer; test reforzado para que no vuelva a colarse en silencio. Tags (ya en schema, sin UI) ganan `TagsInput` + columna badge + infolist. 2 centinelas del baseline invertidos y declarados (picker sin scope → con scope; `status: pending` → `status: new`). 12 tests nuevos (`LeadInboxWorkflowTest`, harness CLA-457, sin datos reales de Bertels — D10). Regresión `Modules/Website` 162/162 (453 assertions), `Modules/Core`+`ClaesenBaseline` 316/316 (1065 assertions). Detalle completo en `CLAUDE.md` → «F4 — Leads y privacidad» → CLA-477. **Pendientes de F4:** CLA-476 (retención/RGPD) — implementable ahora; CLA-474/CLA-475 esperan decisión externa del usuario. Sin push, sin deploy.

> Última actualización: 2026-09-20 (anterior) — **Programa multiempresa Electro Bertels: F4 avanzando — CLA-473 Done (email transaccional y trazabilidad de entrega).** Segundo bug real del ADR (§F4/CLA-473) confirmado y corregido: `MicrosoftGraphTransport::getPayload()` tomaba el nombre del remitente **siempre** de `config('mail.from.name')`, ignorando el `From` del propio Mailable — fix de 2 líneas (`$email->getFrom()[0]->getName() ?: config(...)`). `sites` gana 3 columnas nullable de override (`mail_from_address`/`mail_from_name`/`mail_notification_email`, mismo patrón D7 que los webhooks de CLA-472) — todas `null` para Claesen, cero cambio de comportamiento. `NewConsultationRequestMail`/`ConsultationConfirmationMail` (esta última nueva — antes no existía NINGUNA confirmación al cliente, solo el aviso interno) fijan `From` desde el site real; la plantilla interna ya no hardcodea "Claesen Verlichting" en el header. **`ConsultationEmailDelivery`** (tabla + modelo nuevos) da estado observable `queued/sent/failed` por cada e-mail que dispara una consulta — antes solo un `Log::error()` de una línea. `SendConsultationEmailJob` (nuevo, `ShouldQueue`) reemplaza el `Mail::send()` síncrono dentro de `DB::afterCommit`; **`$tries=1`, sin backoff automático de Laravel, decisión deliberada** — `SyncQueue::push()` (confirmado en vendor) relanza cualquier excepción no capturada, y bajo `QUEUE_CONNECTION=sync` (tests) eso rompería la garantía de CLA-532 de que el 201 nunca falla; el job captura todo y nunca relanza, en `sync` o en `database` real. Recuperación vía `website:retry-failed-consultation-emails` (nuevo, programado cada 15 min), no reintento automático. `last_error` nunca interpola `$e->getMessage()` (solo el nombre de la clase de excepción) — verificado con un test que busca explícitamente PII en el campo. 11 tests nuevos + `ConsultationEmailTest` reescrito para afirmar ambos correos. Regresión `Modules/Website` 150/150 (435 assertions), `Modules/Core`+`ClaesenBaseline` 316/316 (1064 assertions), `MicrosoftGraphTransportPayloadTest` 2/2. Smoke real vía `artisan tinker` contra la DB de dev: dispatch real a la cola `database`, ejecución manual del job confirma la transición a `sent`. Detalle completo en `CLAUDE.md` → «F4 — Leads y privacidad» → CLA-473. **Pendientes de F4:** CLA-476 (retención/RGPD), CLA-477 (bandeja de leads aislada) — implementables ahora; CLA-474 (cuarentena/antimalware) y CLA-475 (antispam/Turnstile) esperan decisión externa del usuario. Sin push, sin deploy.

> Última actualización: 2026-09-20 (anterior) — **Programa multiempresa Electro Bertels: F3 completa; F4 (Leads y privacidad) arrancó — CLA-478 Done.** **CLA-478 (`5aa668c`+`89b8bd4`, Done)** — `ConsultationService::handlePublicIntake()` es ahora el único flujo canónico que llaman `/consultations` y `/contact-email` (antes solo uno de los dos creaba un lead en Prospects, y `ConsultationController` filtraba `$e->getMessage()` directo en la respuesta pública — ambos hallazgos reales, corregidos). `LeadSourceNormalizer` (nuevo) deriva `source`/UTM 100% en servidor (utm_source explícito o el hostname del Referer, nunca lo que manda el cliente). Idempotencia opt-in vía header `Idempotency-Key` (respuesta cacheada en forma array, nunca un modelo vivo — mismo cuidado que CLA-471/CLA-522). `site_id` ya no está hardcodeado a Claesen (`Site::claesenId()` fijo en el código, con un comentario literal "per-site routing is F3/F4") — ahora se deriva del site real resuelto, Prospects solo se alimenta para Claesen (ADR D11). **2 bugs reales preexistentes encontrados y corregidos, sin relación con el alcance original:** `LeadService::persistContactLead()` nunca seteaba `prospects_prospects.region_id` (NOT NULL, nunca antes ejercitado por un test end-to-end real) — fix: fallback a la región 'Brussel' por slug, mismo criterio que la propia migración que hizo la columna obligatoria ya usaba en su backfill; y `prospects_regions` (sembrada solo por migración) necesitaba entrar en `tests/TestCase.php`'s `$exceptTables`, mismo patrón exacto ya visto en CLA-468/CLA-547 (una clase `DatabaseTruncation` la vaciaba, rompiendo el ticket en la corrida de suite completa). 8 tests nuevos + spec OpenAPI actualizado. Regresión `Modules/Website` 140/140, `Modules/Prospects` 32/32, `Modules/Core`+`ClaesenBaseline` 316/316. Detalle completo en `CLAUDE.md` → «F4 — Leads y privacidad» → CLA-478. **Pendientes de F4:** CLA-473 (email transaccional/trazabilidad), CLA-476 (retención/RGPD), CLA-477 (bandeja de leads aislada) — implementables ahora; CLA-474 (cuarentena/antimalware) y CLA-475 (antispam/Turnstile) esperan una decisión externa del usuario (herramienta de antivirus, credenciales Cloudflare) antes de poder avanzar del todo. Sin push, sin deploy.

> Última actualización: 2026-09-20 (anterior) — **Programa multiempresa Electro Bertels: F3 (Fase 3 — Contenido dinámico y publicación) queda COMPLETA — los 6 tickets (CLA-467/468/469/470/471/472) están en Done.** **CLA-472 cerrado por verificación, sin código nuevo** (mismo patrón que el cierre de CLA-460): los 3 criterios que el tramo `aaa2e65` había dejado abiertos ya estaban satisfechos por trabajo previo de la misma cadena, confirmado leyendo el código real, no asumido — HMAC+timestamp con `signature_tolerance_seconds` ya documentado como el contrato que le toca al receptor (frontend Astro, repo separado, fuera de este proyecto); reintentos ya implementados en `TriggerStaticSiteRebuildJob` (`$tries=3`, backoff 30/60/120s); UI de estado + reintento autorizado ya real (`PublicationStatusWidget` con polling 15s, acción `publish_now` en `ListProjects`) — la variante multi-sitio sigue bloqueada por D10 (sin sitio real de Bertels), mismo criterio que el resto del programa. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-472. **Sigue F4** (leads/privacidad, CLA-473→478) y **P6/P7** (bloqueados por staging real — CLA-530/531/525). Sin push, sin deploy.

> Última actualización: 2026-09-20 (anterior) — **Programa multiempresa Electro Bertels: F3 — 5 de 6 tickets Done (CLA-467/468/469/470/471), 1 parcial (CLA-472, sigue en Backlog).** **CLA-471 (`ae32269`, Done)** cierra los 3 criterios que quedaban: caché/ETag (`SetPublicApiCacheHeaders`, nuevo middleware — ETag por hash del cuerpo + `Cache-Control: public, max-age=60` en todo GET exitoso, 304 real en `If-None-Match` coincidente), errores públicos sanitizados (`bootstrap/app.php`'s `withExceptions()` — hallazgo real verificado antes de corregir: con `APP_DEBUG=true`, el valor real de `.env`, un 404 en `/v1/website/*` filtraba el stack trace completo con rutas de `vendor/`; ahora sanitiza a `{"message":...}` con el status correcto sin importar `APP_DEBUG`, `ValidationException` se deja intacto) y OpenAPI/contract tests (`docs/api/website-v1-openapi.yaml` + `OpenApiContractTest`, que valida el spec contra las rutas reales en ambas direcciones). **Bug real encontrado verificando esto, sin relación con el alcance del ticket:** `SiteSetting::cachedForCurrentSite()`/`Announcement::cachedActiveForCurrentSite()` (CLA-469) cacheaban un `Collection` de Eloquent directo — bajo el store `database` real de este repo (`cache.serializable_classes => false`), `unserialize()` nunca reconstruye esa clase y la siguiente lectura truena con `TypeError` (confirmado en logs reales; invisible en los tests de CLA-469 porque `phpunit.xml` usa `CACHE_STORE=array`). Mismo patrón y mismo fix que CLA-522 (`EmployeeDashboardRankingService`): cachear la forma array, rehidratar con `hydrate()` en la lectura. 9 tests nuevos + 2 herméticos de bloqueo de serialización. Regresión `Modules/Website` 132/132, `Modules/Core` 238/238, `ClaesenBaseline` 78/78 — snapshot de rutas regenerado, puramente aditivo. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-471 (cont.). Solo queda **CLA-472** (Backlog, HMAC anti-replay + UI de reintento multi-sitio pendientes) para que F3 quede completa. Sin push, sin deploy.

> Última actualización: 2026-09-20 (anterior) — **Programa multiempresa Electro Bertels: F3 avanzando — 4 de 6 tickets Done (CLA-467/468/469/470), 2 parciales (CLA-471/472, ambos siguen en Backlog en Linear).** **CLA-467 (`e9fdf23`, Done)** — cierra el hallazgo real de esta auditoría: `Project::getApi*Attribute()`, `Modules\Website\App\Http\Resources\ProjectResource` y `PortfolioService::getProjectGallery()` (código muerto, corregido igual) servían el archivo ORIGINAL sin convertir, directo, sin autenticación, en la API pública. `Project::registerMediaCollections()` mueve featured_image/gallery/detail_gallery al disco privado `local` (mismo disco de FieldOps/Safety), conversiones fijadas en `public` — el original ya no tiene ninguna URL pública alcanzable, ni enumerable. 3 conversiones WebP ganan gemelas AVIF (confirmado soportado en este host, byte a byte, no solo por extensión de archivo). `focal_point` centrado por defecto vía `MediaObserver`, expuesto en la API. "Registro de permiso de uso/revisión de privacidad" vía 2 comandos Artisan nuevos — UI de Filament fuera de este cierre a propósito (mismo motivo que alt/caption: `SpatieMediaLibraryFileUpload` no soporta campos por-archivo). `website:regenerate-media` gana guard de solapamiento; `website:migrate-media-to-private-disk` (nuevo, dry-run, idempotente) es el vehículo real de migración para cuando esto se despliegue. Filament admin: `->conversion('thumb')` en los 3 uploads para que el preview siga funcionando (el original ya no resuelve URL). 15 tests nuevos + 2 centinelas invertidos declarados (la clave `original` se elimina a propósito). Regresión `Modules/Website` 123/123, `Modules/Core` 238/238, `ClaesenBaseline` 78/78 — sin drift de rutas/esquema. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-467. F3 no está completa todavía: CLA-471/472 siguen en Backlog con alcance parcial (site-scoping/publicación por sitio ya funcionan; OpenAPI/ETag/errores sanitizados y HMAC anti-replay/UI de reintento quedan pendientes). Sin push, sin deploy.

> Última actualización: 2026-09-20 (anterior) — **Programa multiempresa Electro Bertels: F3 avanzando — 5 de 6 tickets (CLA-470/468/469 Done, CLA-471/472 parciales).** **CLA-469 (`6c7bd7d`, Done)** — SiteSettings (catálogo fijo key/value: horarios/teléfono/email/dirección/redes sociales, whitelist en `config('website.site_settings.allowed_keys')`, nunca un enum de BD) + Announcement (contenido real en lista, traducible, lifecycle draft/published/archived + ventana de fechas) — dos conceptos deliberadamente separados para respetar "no se convierte el backoffice en page builder". API pública `GET /v1/website/settings`/`announcements` cacheada por sitio con invalidación en save/delete, nunca expone campos internos de `Site`. Panel: `SiteSettingsPage` (formulario de slots fijos) + `AnnouncementResource` (CRUD real), ambos hardcodeados a Claesen. Bug real corregido: `Tables\Actions\EditAction`/`DeleteAction` (namespace Filament 3/4) → `Filament\Actions\*` (v5 real). Verificado: 20 tests nuevos (incluye aislamiento cross-site con el harness CLA-457, fixture descartada, D10 intacto); regresión `Modules/Website` 108/108, `Modules/Core` 238/238, `ClaesenBaseline` 78/78 con snapshot de rutas regenerado de forma puramente aditiva (389→395 rutas). Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-469. Sigue F3 (CLA-467 originales privados, único ticket F3 sin empezar), F4, P6/P7 (bloqueados por staging real — CLA-530/531/525). Sin push, sin deploy.

> Última actualización: 2026-09-19 — **Programa multiempresa Electro Bertels: F2 sustancialmente cerrado; F3 avanzando — 4 de 6 tickets (CLA-470/468 Done, CLA-471/472 parciales).** F1/P5 siguen completos (CLA-457, `ddb8e93`). **CLA-468 (`4a9c101`, Done)** — reemplaza `Modules\Website\App\Enums\ProjectCategory` (enum PHP global, eliminado) por un catálogo real `website_project_categories` scoped por `site_id`, para que Bertels pueda tener sus 6 categorías (residencial/empresa/industria/KNX-Qbus/iluminación/renovación) sin compartir enum con las 3 de Claesen. `website_projects.category` no cambió de tipo/valores — cero riesgo de migración de datos, solo cambió qué gobierna qué slugs son válidos. Traducciones de Claesen copiadas verbatim de `lang/*/website.php`. Hallazgo real: `tests/TestCase.php`'s `$exceptTables` (de CLA-547, protege `organizations`/`sites` de `DatabaseTruncation`) necesitaba agregar `website_project_categories` también — sin eso, un test `DatabaseTruncation` vacía el catálogo de Claesen para el resto de la suite. Verificado: 8 tests nuevos + regresión `ClaesenBaseline`+`Website` 174/174 (627 assertions) + `Core` 238/238. Cierra COMPLETO, como CLA-470. Resumen de lo cerrado antes en esta sesión: CLA-558 Done, CLA-471/472 parciales (site-scoping API+publicación), CLA-465/464 parciales (auditoría/MFA), CLA-470 Done (contrato de medios). Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels». Sigue F3 (CLA-467 originales privados, CLA-469 SiteSettings), F4, P6/P7 (bloqueados por staging real — CLA-530/531/525). Sin push, sin deploy.

> Última actualización: 2026-09-18 — **Programa multiempresa Electro Bertels: CLA-560 (F2/P5d) — destinatarios de notificaciones organización-conscientes. P5 (enforcement por capas) queda completo.** Commit `2ec5204`: nuevo `User::scopeInOrganization()` (no-op con `organizations.enforce` apagado, default a Claesen con el flag activo) aplicado a los 7 sitios exactos que el baseline P0 (`AccessControlContractTest`) ya congelaba como el hueco CRITICAL — FieldOps (`MaintenanceRequestService`/`MaintenanceRequestAlertService`), Safety (`CheckSafetyComplianceCommand`/`ChecklistObserver`/`InspectionReminderService`/`InspectionController`), Mailing (`CheckDeliverabilityAlertsCommand`) — todos seleccionaban destinatarios por rol Spatie global, sin organización. Centinela invertido declarado (las 7 aserciones de source ahora exigen `->inOrganization()`). Cobertura honesta: `ChecklistObserver`/`InspectionReminderService` sin test dedicado de su rama de notificación (hallazgo preexistente, documentado, no corregido — el cambio en sí es mecánico). Verificado: `UserInOrganizationScopeTest` (nuevo) **4/4/8 assertions**; `Modules/Core/tests`+`ClaesenBaseline` completos **286/286/1032 assertions**; regresión FieldOps/Safety/Mailing **63/63/266 assertions** — cero cambio con el flag apagado. **Con esto, las 4 capas de P5 (paneles/logins, rutas de módulos, Gate::before, destinatarios) están completas.** Pendiente con ticket propio: CLA-558 (policies organización-conscientes en `FieldOpsTenantService`, aplazado por riesgo sobre tenant-isolation delicada). Sigue P6 (recursos reales de Bertels, F3/F4) y P7 (flag en producción, bloqueado por staging real — CLA-530/531/525). Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-560. Sin push, sin deploy.

> Última actualización: 2026-09-18 — **Programa multiempresa Electro Bertels: CLA-557 (F2/P5c) — `Gate::before` organización-consciente (ADR D5).** Commit `80a5f2b`: `Gate::before` en `AppServiceProvider.php` deja de dar `true` incondicional a `super_admin` — con el flag activo, un sujeto (instancia/class-string) registrado en el nuevo `config('organizations.owned_models')` (~74 modelos de los 8 módulos de CLA-552) solo bypassa si pertenece a la organización del usuario; un sujeto no registrado sigue igual que hoy (allowlist deliberada, no blocklist); sin sujeto, solo bypassa si la ability está en `platform_abilities` (vacía). Con el flag apagado (default hoy), byte-for-byte el comportamiento original. **Hallazgo real, con ticket propio (`CLA-558`), no corregido acá por decisión explícita del usuario:** esto NO logra aislamiento real para FieldOps — sus policies dependen de `FieldOpsTenantService::hasBroadAccess()` = permiso Spatie GLOBAL del rol (D2), que un futuro `super_admin` de Bertels tendría igual. Redisñar ese servicio es un cambio de mayor riesgo sobre tenant-isolation ya delicada (CLA-266/364/369/375/377/496/497), queda para ticket aparte. Centinela invertido declarado en `AccessControlContractTest` (P0, ya anticipaba este hueco como "CRITICAL" en su propio comentario). Verificado: `GateBeforeOrganizationEnforcementTest` (nuevo) **7/7/8 assertions**; `Modules/Core/tests`+`ClaesenBaseline` completos **282/282/1024 assertions**; regresión FieldOps **70/70/252 assertions** — cero cambio con el flag apagado. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-557. Sigue el resto de P5c ("selects y búsqueda"). Sin push, sin deploy.

> Última actualización: 2026-09-18 — **Programa multiempresa Electro Bertels: CLA-552 (F2/P5b) — middleware `organization:claesen` en 8 módulos API.** Commit `e807b41`: `Modules/Core/Http/Middleware/RequireOrganization` (alias `organization:{slug}`, gateado por `config('organizations.enforce')`) aplicado a las rutas `auth:sanctum` de FieldOps/Safety/Employee/Mailing(solo `mailings`)/Intelligence/Performance/Prospects/Cafca — el hueco que P5a (gate de panel Filament) no cubría, ya que estas rutas las consumen apps de campo (Claesen-Sport, Safety PWA) directamente por token Sanctum. Excluidos a propósito: Core, Website (`site_id`-scoped), Analytics (`/api/v1/events` público). Hallazgo: 5 de los 8 módulos gateaban `apiResource index()` scaffolding nwidart sin fleshear, nunca conectada a lógica real — solo FieldOps/Safety/Employee protegen tráfico real hoy, documentado para no sobrestimar el alcance. `config('organizations.owned_modules')` poblado (D9). Centinela invertido declarado en `OrganizationsSchemaTest` (misma clase que CLA-547 ya había invalidado antes). Snapshot de rutas regenerado (160 líneas, puramente aditivas). Verificado: `ModuleOrganizationEnforcementTest` (nuevo) **26/26/27 assertions**; `Modules/Core/tests`+`ClaesenBaseline` completos **275/275/1013 assertions**; regresión dirigida FieldOps/Employee/Safety **61/61/225 assertions** — cero cambio con el flag apagado. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-552. Sigue P5c (`Gate::before`, policies, selects, búsqueda). Sin push, sin deploy.

> Última actualización: 2026-09-18 — **Programa multiempresa Electro Bertels: CLA-460 cerrado (Done) — sin código nuevo, por verificación.** El último criterio pendiente ("resolución de `OrganizationContext` en jobs/cola") se cerró verificando contra el vendor instalado (no asumiendo): `QueueServiceProvider.php:263`/`Worker.php:246-247` confirman que el `queue:work` persistente (`QUEUE_CONNECTION=database`, corrige la nota de CLA-522 que asumía `sync`) resetea el binding `scoped` de `OrganizationContext` después de cada job — sin fuga posible entre jobs, sin necesitar middleware propio. Cero `Job` del repo consume la clase (`grep` vacío) — construir esa capa ahora sería scaffolding sin uso; el ADR (D6, corregido en este cierre — antes describía el middleware/`Queue::after` en presente, como si ya existiera) documenta que se construye recién con el primer job real que lo necesite. Los 6 criterios de aceptación de CLA-460 quedan cubiertos: contexto resuelto/revalidado (P4, `abc8a9c`), indicador visual permanente (satisfecho por diseño — logo/color por panel, D1), sin opción "todas las empresas" (trivial, sin dropdown), cambio de contexto auditado (`eedfeef`), URL de otro tenant → 403 (P5a, `692d780`). **P4 del programa queda completa**; P5b–d y P6 (recursos reales de Bertels) siguen sin empezar. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-460 — cierre. Sin push, sin deploy, sin código.

> Última actualización: 2026-09-18 — **Programa multiempresa Electro Bertels: CLA-460 (cont.) — P5a, enforcement real del panel admin por organización.** Commit `692d780`: `User::canAccessPanel()` gana, dentro del branch `'admin'` y gateada por `config('organizations.enforce')` (default apagado en todo entorno), el rechazo de cualquier usuario cuya `organization_id` no sea la de Claesen — mismo punto de extensión que ya usa la rama `'bertels'` de CLA-549 (`Filament\Http\Middleware\Authenticate` llama a este método por panel), no una middleware nueva. Elegido explícitamente por el usuario vía `AskUserQuestion` frente a "solo resolución en jobs/cola" o "pausar el programa". `Modules/Core/tests/Feature/PanelOrganizationEnforcementTest.php` (nuevo, 7 tests) usa una organización **fixture** creada y descartada dentro de `RefreshDatabase` — ningún usuario real de Bertels existe (D10). **Incidente operativo de la sesión:** el daemon de Docker Desktop/WSL2 quedó sin respuesta a mitad de la verificación anterior (mismo patrón de `feedback_sail_docker`/CLA-496); al reiniciarlo se encontró que este worktree **nunca tuvo `.env`** — sin él, `DB_CONNECTION` cae al default real del proyecto (`sqlite`, no `mysql`), así que las corridas previas de `migrate:fresh` de esta sesión escribían en silencio a un SQLite suelto en la raíz (`./testing_p5a`, borrado) en vez de MySQL real; no fue corrupción por el crash, aunque se descartó recreando el volumen de MySQL antes de dar con la causa real. Se generó un `.env` real (bootstrap estándar de Sail, gitignored). Verificado tras el fix (MySQL 8.4 real, `migrate:fresh` limpio, 179 migraciones/107 tablas): `PanelOrganizationEnforcementTest` **7/7/8 assertions**; regresión (+`BertelsPanelAccessTest`+`PanelAccessMatrixTest`) **62/62/269 assertions**; `ClaesenBaseline` **77/78** (único fallo, `RouteInventorySnapshotTest`, drift de vendor Livewire `livewire.min.js`→`livewire.js`, confirmado idéntico contra HEAD sin este cambio vía `git stash`). Pint: `User.php` mismo set de 3 fixers preexistentes que HEAD, sin deuda nueva. CLA-460 sigue `In Progress` — solo falta resolución de `OrganizationContext` en jobs/cola. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-460 (cont.) P5a. Sin push, sin deploy.

> Última actualización: 2026-09-18 — **Programa multiempresa Electro Bertels: CLA-460 (cont.) — selector auditado de super_admin entre paneles.** Commit `eedfeef`: `GET /switch-panel/{panel}` (`Modules/Core/Http/Controllers/SwitchPanelController`, fuera de cualquier panel Filament, `super_admin` únicamente) + `NavigationItem` en ambos providers (`->visible()` por rol) + auditoría vía `activity()` **sin subject** (primer uso de esta forma en el repo — no hay columna `activity_log.organization_id` todavía, ni org/sitio de Bertels por D10, así que va en `properties` JSON). Cierra "el resto de P4 (AuditLogger)" que CLA-460 dejaba pendiente y el criterio de salida de P6 ("selector auditado"). Verificado: `PanelSwitchTest` (nuevo) 6/6; regresión `BertelsPanelAccessTest`+`PanelAccessMatrixTest` 55/55; `ClaesenBaseline` 78/78/384; `Modules/Core/tests` completo **164/164, 0 fallos**. Snapshot de rutas regenerado (declarado, 386→387). **Verificación visual real en navegador** (mismo entorno de preview ya corriendo en `localhost:8123` para el usuario): click real de ida y vuelta entre paneles, confirmado en `activity_log` real que ambos switches quedaron registrados con `causer_id`/`from_panel`/`to_panel` correctos. CLA-460 sigue `In Progress` — solo faltan "URL de otro tenant → 404/403" (P5) y resolución en jobs/cola. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-460 (cont.). Sin push, sin deploy.

> Última actualización: 2026-09-18 — **Continuación de CLA-549: `npm run build` + verificación visual real + fix de bug encontrado.** Assets de Vite construidos (`npm ci && npm run build`, ausentes desde hacía tiempo en este worktree). Verificación real con `php artisan serve` + Selenium + un usuario `super_admin` QA temporal (todo eliminado al terminar) encontró un bug real: `bertels-brand-logo-light.jpg` (lockup vertical 265×360) quedaba casi ilegible en el topbar en modo claro — sin equivalente horizontal entre los assets entregados. Fix (commit `00df084`): modo claro usa el ícono cuadrado `bertels-favicon.jpg` (472×452, mismo archivo del favicon) en vez del lockup — reverificado visualmente, ambos modos correctos. Regresión: `BertelsPanelAccessTest` 7/7 + `PanelRegistrySnapshotTest` 3/3. Además se corrigió el hallazgo colateral de `OrganizationsSchemaTest` (CLA-458, desactualizado por CLA-547) — commit `00fc2f4`, `Modules/Core/tests` completo ahora **158/158, 0 fallos**. Nuevo ticket **CLA-550 = Done** con esta evidencia. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-549. Sin push, sin deploy.

> Última actualización: 2026-09-18 — **Programa multiempresa Electro Bertels: primer spike de P6 completo.** **CLA-549 = Done** (commit `515a8a2`): segundo panel Filament (`App\Providers\Filament\BertelsPanelProvider`, `id('bertels')`/`path('bertels')`, sin `->default()`), marca de `electrobertels.md` aplicada (`#EE7203`/`#121212`, 3 logos entregados por el usuario copiados sin editar bytes), **cero recursos/páginas/widgets propios** — dashboard realmente vacío. Autorización vía `User::canAccessPanel()` (extensión nativa que `Filament\Http\Middleware\Authenticate` ya invoca por panel), no una middleware nueva: solo `super_admin` pasa, porque ningún usuario real de Bertels existe todavía (D10). Reutiliza el mismo `Login` (Azure) que Claesen sin tocar `MicrosoftAuthController` — confirma que el callback funciona igual bajo un segundo panel. **Hallazgo real de la verificación:** `Filament\Navigation\NavigationManager` está bindeado `scoped`, y Laravel solo lo limpia en fronteras de job de cola — nunca tras un request HTTP normal; en producción (PHP-FPM sin Octane) esto nunca es un riesgo real porque cada request es un proceso nuevo, pero dentro de un mismo método de test PHPUnit (que reutiliza el contenedor entre `->get()` sucesivos) sí se filtra estado de un panel a otro — los tests que verifican ambos paneles se dividieron en métodos separados. Centinela del baseline invertido, declarado: `PanelRegistrySnapshotTest` ahora exige 2 paneles, `admin` queda byte-a-byte idéntico (snapshot de rutas: 383→386, solo 3 líneas nuevas de `bertels`). Verificado: `BertelsPanelAccessTest` (nuevo) 7/7; `PanelRegistrySnapshotTest` 3/3; `ClaesenBaseline` completo 78/78/384 assertions (antes 378); regresión con `PanelAccessMatrixTest` 58/58/270 assertions. **Verificación visual real en navegador NO realizada** — este worktree no tiene `public/build/manifest.json` (mismo problema de entorno ya documentado repetidamente). **Hallazgo colateral, fuera de este ticket:** `OrganizationsSchemaTest` (CLA-458) quedó desactualizado por CLA-547 y falla en la suite completa de `Modules/Core/tests` — documentado en `known-risks.md`, no corregido acá para no mezclar tickets. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-549. Sin push, sin deploy.

> Última actualización: 2026-09-18 — **Programa multiempresa Electro Bertels: P3 completa (P3a+P3b).** **CLA-548 = Done** (commit `e1aee21`): segunda mitad de P3, cierra lo que CLA-547 dejó fuera a propósito con este mismo nombre. `site_id` nullable + FK `restrictOnDelete` **+ índice `UNIQUE`** en `website_publication_states` (2 migraciones estructura+datos, backfill vía `DB::table()`) — a diferencia de `website_projects`/`website_consultation_requests`, el ADR (D3) marca esta tabla específicamente como singleton-por-sitio, no `site_id` plano: hoy era `PublicationState::current()` con `find(1)` hardcodeado, sin ninguna FK entrante. `PublicationState::current(?int $siteId = null)` reemplaza esa PK fija por `firstOrCreate(['site_id' => $siteId ?? Site::claesenId()], ...)` — firma retrocompatible, cero cambios en los 4 call sites reales (`StaticSitePublicationService`, `TriggerStaticSiteRebuildJob`, `PublicationStatusWidget`, `ListProjects`). `BelongsToSite` (inerte, D4) aplicado, mismo patrón que `Project`/`ConsultationRequest`. El `down()` dropea la FK antes que el único **por diseño** (mismo patrón MySQL 1553 que CLA-547/CLA-523 ya documentaron) — esta vez sin necesitar un segundo intento. 2 centinelas del baseline invertidos, declarados: `AccessControlContractTest` ya no exige el string `static::find(1)`, y `WebsiteSiteScopingTest` ya no exige la ausencia de `site_id` en esta tabla. Verificado: `WebsiteSiteScopingTest` 12/12; `Modules/Website/tests` completo 75/75 (antes 72/72); `tests/Feature/ClaesenBaseline` completo 77/77/378 assertions (antes 376); ciclo `migrate → rollback --step=2 → migrate` verificado end-to-end contra MySQL real, sin 1553. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-548. Sin push, sin deploy.

> Última actualización: 2026-09-18 — **Programa multiempresa Electro Bertels: P3a cerrado.** **CLA-547 = Done** (commit `d5b6b0a`): `site_id` nullable + FK `restrictOnDelete` en `website_projects`/`website_consultation_requests` (2 migraciones estructura+datos, backfill vía `DB::table()` por los observers de `Project`/`ConsultationRequest`) + `UNIQUE(slug)` → `UNIQUE(site_id, slug)` + `BelongsToSite` inerte (D4) + `Site::claesenId()` fijado en los 3 caminos reales de creación (`CreateProject`/`CreateConsultationRequest`/`ConsultationService::store()`). `website_publication_states` queda deliberadamente fuera (→ P3b, ticket propio). Dos bugs reales encontrados y corregidos en la verificación: (1) MySQL 1553 en el `down()` de la 3ª migración, mismo patrón que CLA-523 documentó — fix con índice plano intermedio; (2) `tests/TestCase.php` gana `exceptTables = ['organizations', 'sites']` — las 3 clases `DatabaseTruncation` de `Modules/Website` perdían la fila sembrada de Claesen entre tests y `Site::claesenId()` reventaba con 500 desde el 2º test; el fix va en el `TestCase` base porque protege cualquier `DatabaseTruncation` futuro del resto del programa, no solo estos 3 archivos. Verificado: `WebsiteSiteScopingTest` 9/9; `Modules/Website/tests` completo 72/72; `tests/Feature/ClaesenBaseline` completo 77/77/376 assertions (idéntico a P1/P2/P4 salvo la +1 assertion del centinela invertido declaradamente); ciclo `migrate → rollback → migrate` de las 3 migraciones verificado end-to-end contra MySQL real. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-547. Sin push, sin deploy.

> Última actualización: 2026-09-18 — **Programa multiempresa Electro Bertels: CLA-460 (parcial, P4 del ADR) comiteado, ticket sigue In Progress.** Commit `abc8a9c`: `Modules\Core\Http\Middleware\ResolveOrganizationContext` (nuevo, appended al grupo `web` en `bootstrap/app.php` junto a `UpdateUserActivity`) — resuelve `OrganizationContext` para todo request autenticado (panel y Livewire), sigue 100% inerte. Cubre solo 2 de los 6 criterios de aceptación del ticket ("resuelto en servidor" + "revalidado por request"); el resto (indicador visual/selector `super_admin`/auditoría de cambio → P6, "todas las empresas" oculto → P6, 404/403 de otro tenant → P5) queda pendiente a propósito. **Nota de secuencia:** esto es técnicamente P4 del ADR, adelantado antes de P3 (`site_id` en Website, sin empezar) porque `OrganizationContext` resuelve directo desde `users.organization_id` sin depender de `site_id` — decisión explícita del usuario, documentada en el ADR §6 para no dejar el orden divergiendo en silencio. Verificado: filtro propio+regresión **29/29 / 87 assertions**; `tests/Feature/ClaesenBaseline` completo **77/77 / 375 assertions** — idéntico a P1/P2 pese al middleware global nuevo; `Modules/Core/tests` completo **150/151** (único fallo, entorno, ya documentado). Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-460. Sin push, sin deploy.

> Última actualización: 2026-09-18 — **Programa multiempresa Electro Bertels: P2 cerrado.** **CLA-459 = Done** (commit `476f68d`): columna nullable `users.organization_id` (FK `restrictOnDelete`, `NOT NULL` recién en P7), comando idempotente/resumible `core:backfill-user-organizations {--dry-run} {--apply}` (registrado en `CoreServiceProvider::registerCommands()` — se encontró que este módulo no tiene auto-discovery de comandos, y de paso que `core:link-users-to-employees` de CLA-171 nunca estuvo registrado tampoco, bug preexistente consignado en `known-risks.md`, fuera de alcance de este cierre), los 6 puntos de creación real de usuario fijados a Claesen (`CreateUser`, `ClientContactInvitationService::invite()`, `RolesAndPermissionsSeeder`, 3 usuarios QA — Azure OAuth confirmado que **nunca** crea usuarios, no es una vía real), default de `UserFactory` resolviendo Claesen, y `Modules\Core\Services\OrganizationContext` (resolve-only, `scoped` per ADR D6, sin consumidores todavía). Verificado contra MySQL 8.4 real (`migrate:fresh` limpio): filtro P2+regresión **39/39 / 105 assertions**; `tests/Feature/ClaesenBaseline` completo **77/77 / 375 assertions** — idéntico a P1, cero diferencia de comportamiento para Claesen; `Modules/Core/tests` completo **147/148** (el único fallo es el mismo `ViteManifestNotFoundException` de entorno ya documentado, sin relación con este cambio). Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-459. Fuera de este cierre, a propósito: `NOT NULL` (P7), UI de selección de organización (P6), scopes/enforcement (P3-P5), harness de aislamiento (CLA-457), contexto/selector de `super_admin` (CLA-460). Sin push, sin deploy.

> Última actualización: 2026-09-17 — **Programa multiempresa Electro Bertels: F0 completo, P1 cerrado.** **CLA-458 = Done** (commit `4c3288a`): tablas `organizations`/`sites` en `Modules/Core/database/migrations/` (dos migraciones separadas, estructura + alta idempotente de Claesen vía `DB::table()`, D7), modelos `Organization`/`Site` + factories, `config/organizations.php` (scaffolding inerte, `enforce=false`), `Modules/Core/tests/Feature/OrganizationsSchemaTest.php` (14 tests). **Estructura pura — cero cambio de comportamiento para Claesen**, nada en la aplicación lee estas tablas todavía. Verificado contra MySQL 8.4 real (`migrate:fresh` limpio): `OrganizationsSchemaTest` **14/14 / 52 assertions**; suite `tests/Feature/ClaesenBaseline` completa **77/77 / 375 assertions** — cero diferencia en rutas/scheduler/panel/autorización/API pública de Claesen, el criterio de salida de P1 que fija el ADR. Detalle completo en `CLAUDE.md` → «Programa multiempresa Electro Bertels» → CLA-458. Fuera de este cierre, a propósito: harness de aislamiento (CLA-457), backfill de `users.organization_id` (CLA-459), contexto/selector de `super_admin` (CLA-460) — siguientes tramos de F1, sin empezar. Sin push, sin deploy.

> Última actualización: 2026-09-17 — **Programa multiempresa Electro Bertels, F0/P0 entregado** (auditoría + ADR + baseline de regresión; sin código de aplicación, sin migraciones, sin deploy). Rama `electrobertels/trunk` sobre `ef2bc6e`. **Tickets Linear: CLA-451** (auditar arquitectura y flujos), **CLA-452** (plan de migración, backfill y rollback) y **CLA-453** (aprobar el ADR), los tres movidos a `In Progress` con la evidencia publicada como comentario; hito «Fase 0 — Descubrimiento y ADR» del proyecto «Electro Bertels — Web y backoffice multisite». **CLA-454** (requisitos no funcionales, RGPD y Definition of Done) queda en `Backlog`, fuera de este entregable. **Decisión aprobada por el usuario:** opción C — **un panel Filament por empresa dentro del mismo monolito** (Claesen sigue en `/`, Bertels en `/bertels`), una sola app, una BD, un despliegue y una autenticación. Ajustes exigidos e incorporados al ADR: (1) secuencia **estructura → contexto → autorización → enforcement** — en P3 los traits entran **inertes** (el scope consulta `config('organizations.enforce')`), el fail-closed solo se activa en P5; (2) **`site_id` como única fuente de verdad** en las entidades site-owned (organización derivada de `sites.organization_id`); si algún día se desnormaliza `organization_id`, solo con FK compuesta contra `sites (id, organization_id)`, nunca dos FKs sueltas; (3) `Gate::before` **primero frontera de organización, después privilegio** — «sin modelo» **no** es global, hay lista explícita `platform_abilities` (arranca vacía porque hoy todas las comprobaciones pasan modelo o class-string); (4) `OrganizationContext` como binding **`scoped`**, nunca `singleton` (el worker reinicia scoped entre jobs, `Worker.php:246`), con middleware de job + `Queue::after` como defensa; (5) migraciones de **estructura y datos separadas** (`create_organizations_and_sites_tables` + `seed_claesen_organization_and_site`), siempre versionadas porque `deploy.sh` solo corre `migrate --force`; (6) **MFA como gate previo al primer usuario real de Bertels** (Filament 5 lo soporta nativo; hoy no hay MFA configurado). Corregido además el mapa de hosts: `backend.claesen-verlichting.be` es el host público del túnel (API + enlace «Website V1 Demo»), **no** el frontend Astro de producción, cuyo hostname no está en la configuración versionada (a confirmar). **Entregado:** `docs/ai/adr-multi-organization.md` (decisiones D1–D10, plan P0→P7+F3/F4, rollback por fase, 7 preguntas abiertas), 6 tests de caracterización en `tests/Feature/ClaesenBaseline/` con snapshots commiteados (383 rutas, 22 tareas del scheduler, registro del panel: 34 recursos / 15 páginas / 9 widgets / 10 recursos con búsqueda global), y los riesgos del programa en `known-risks.md` (6 CRÍTICOS, entre ellos roles globales, API pública sin sitio, `Gate::before`, destinatarios por rol, publicación singleton y media de FieldOps por id). **Decisiones de alcance (2026-09-17):** (1) de todos los módulos de Claesen **el único que se comparte es Mailing**; (2) **solo transaccional por ahora**. La segunda acota radicalmente la primera: el correo transaccional **no pasa por el dominio de Mailing** (`ConsultationService` → `Mail::mailer('microsoft-graph')->send(new NewConsultationRequestMail(...))`), así que lo compartido es el **transporte** (mailer `microsoft-graph`, `MicrosoftGraphTransport`, `MicrosoftGraphService`, Laravel Mail) y **ninguna tabla `mailing_*` recibe `organization_id`**; campañas, audiencias, supresión, plantillas versionadas, A/B y follow-ups siguen 100 % Claesen, y en P5b Mailing **sí** entra en la lista con `organization:claesen`. Trabajo real que queda, en F4/CLA-473: remitente y nombre visible por sitio (`NewConsultationRequestMail::envelope()` no fija From → cae a `config('mail.from.address')`, y `getPayload()` toma `'name'` de `config('mail.from.name')` siempre, así que un correo de Bertels mostraría el nombre de Claesen), destinatario interno por sitio, plantilla y marca propias, **la confirmación al cliente que hoy no existe**, y permiso de Graph para enviar como el buzón de Bertels. **Tenant de Microsoft 365 confirmado como compartido** (2026-09-17): el envío transaccional de Bertels usa la misma app registration, sin credenciales por organización ni ESP (MAI-026 vuelve a ser solo una decisión de entregabilidad de Claesen). A cambio sube el riesgo en dos puntos: `Mail.Send` de aplicación sin acotar permite enviar como cualquier buzón del tenant (→ Application Access Policy de Exchange + From siempre desde configuración del sitio), y el fallback a `viewer` de `AzureRoleService` deja de ser hipotético — un empleado de Bertels entrando por Microsoft aterrizaría hoy en el panel de Claesen, lo que convierte P5a en prerrequisito duro del alta de usuarios de Bertels. Pendiente de confirmar explícitamente si los usuarios de Bertels también viven en ese tenant. Los bloqueantes de audiencia/supresión/dominio de baja quedan aparcados como riesgo condicional a una futura decisión de campañas. **Regla de hierro registrada:** ningún usuario real de Bertels antes de completar P5. **Verificación (completa, local):** los 4 tests sin BD **21/64 assertions OK**; los 2 con `RefreshDatabase` **56/311 assertions OK** contra un MySQL 8.4 propio de este checkout (`docker compose up -d mysql`, proyecto Docker `electrobertels`, puerto **3310** para no chocar con el 3308 de otro worktree; sin `.env`, pasando `DB_CONNECTION=mysql DB_HOST=127.0.0.1 DB_PORT=3310 DB_USERNAME=sail DB_PASSWORD=password` y un `APP_KEY` generado en memoria). **Suite `Feature` completa: 109 tests / 482 assertions, 0 fallos**, con los 4 «PHPUnit Notices» preexistentes documentados en CLA-524 (solo aparecen en la corrida combinada; `failOnNotice` no está activo). La matriz de acceso derivada de leer el código resultó exacta: 34 recursos × 5 roles y 12 páginas pasaron sin un solo ajuste. Pint **passed** y `php -l` limpio en los 7 archivos nuevos. **Dos hallazgos de la verificación, ya incorporados:** (1) `PanelAccessMatrixTest` usa `withoutVite()` — sin eso falla con `ViteManifestNotFoundException` en cualquier checkout sin `npm run build`, el mismo fallo ambiental que tumbó 84 tests en la 1ª corrida de CI de CLA-525, y el baseline mide autorización, no el pipeline de assets; (2) **sin `.env`, `DB_CONNECTION` cae al default `sqlite` del skeleton de Laravel** — una primera invocación migró a un fichero SQLite de 194 MB en la raíz del repo; abortada y ambos artefactos borrados (árbol limpio), de ahí que el driver deba pasarse siempre explícito en este worktree. Comiteado sin push ni deploy.

